---
# Not a real App, and it never ships — see src/lib/apps.ts. This is the shape no
# real App has yet and every one of them should end up in: launched, with a store
# listing, a page written about it and a published privacy policy. It is here so
# that what happens when an App has *all* of those is decided by a test rather
# than discovered on the day one of them launches.
name: Fixture Launched App
initials: FL
description: A launched App with a page and a policy, so the whole set of rules meets in one place.
platform: Test
status: live
url: https://example.com/fixture-launched-app
order: 97
---

Fixture body copy for an App that has actually launched.
